<import-steps>
  <!-- flag mailhost as async and set up the maildir directories if missing -->
  <import-step
    id="upiqsite.opip-mailhostqueue"
    handler="upiqsite.opip.setuphandlers.enable_smtp_queue"
    title="upiqprojects.opip: enable smtp_queue in MailHost"
    version="1.0"
    >
    Enable queued SMTP sending, such that sending from mailhost is not blocking.
  </import-step>
</import-steps>
